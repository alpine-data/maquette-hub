MERGE (asset:DataAsset { id: '{{ dataAsset.getId() }}', type: '{{ dataAsset.getType() }}' })
MERGE (app:Application { project: '{{ application.getProject() }}', id: '{{ application.getId() }}' })
MERGE (asset)-[c:CONSUMED_BY]->(app)
SET c.tracked = datetime()