// import { selectCreateProjectDomain } from '../selectors';

describe('selectCreateProjectDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
