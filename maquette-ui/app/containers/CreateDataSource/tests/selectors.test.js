// import { selectCreateDataSourceDomain } from '../selectors';

describe('selectCreateDataSourceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
